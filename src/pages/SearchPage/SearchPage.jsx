import React from 'react'

export const SearchPage = ({searchData}) => {
    console.log('search page: ' + searchData)
    return (
        <div>
            <h1>Search page</h1>
        </div>
    )
}
